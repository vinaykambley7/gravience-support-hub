# Local project notes

This project is intended to run as a local codebase, not as a Lovable-hosted app.
Keep edits focused on the repository itself, and make sure environment variables
are configured in the local workspace before running the app.
